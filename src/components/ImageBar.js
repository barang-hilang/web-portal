var React = require('react');

var ImageBar = React.createClass({
  render: function(){
    return(
      <div className="ImageBar">
        <h2>Nanti di isi gambar </h2>
      </div>
    );
  }
});

module.exports=ImageBar;
